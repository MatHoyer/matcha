// import db from '../database/Database';

// export const saveMessage = async (userId : number, receiverId: number, message: string, date: Date) => {
//     const newMessage = await db.message.create({
//         data: {
//             userId: userId,
//             receiverId: receiverId,
//             message: message,
//             date: date,
//         }
//     });
    
//     return newMessage;
// };